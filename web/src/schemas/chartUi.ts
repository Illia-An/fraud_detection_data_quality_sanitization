/** Chart UI modes shared by uiStore, KpiCards, and StoreImpactChart. */
export type ChartScopeMode = 'store' | 'network';

/** Timeline = linear months; YoY = month-of-year overlay across years. */
export type ChartTimeMode = 'timeline' | 'yoy';
